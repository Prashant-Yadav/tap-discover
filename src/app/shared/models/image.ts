import {ImageCategory} from "./image-category";

export class Image {
  category: ImageCategory;
  date_of_entry: string;
  id: number;
  image_link: string;
  price: number;
  source: string;
  time_needed: number;
}
